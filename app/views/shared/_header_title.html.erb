<% if defined?(header_title) && defined?(header_sub_title) %>
  <% content_for :header_title do %>
    <div class="col-lg-12 text-right">
      <h3 class="page-header">
        <%= header_title %>
        <small><%= header_sub_title %></small>
      </h3>
    </div>
  <% end %>
<% end %>