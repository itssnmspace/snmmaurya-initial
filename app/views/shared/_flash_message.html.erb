<% message, message_class = flash[:success].present? ? [flash[:success], "success"] : [flash[:error], "danger"] %>
<% if message.present? %>
  <div class="alert alert-<%= message_class %> flash_message">
    <%= message %>
  </div>
<% end %>